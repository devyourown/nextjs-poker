import { Room } from "@/core/room/Room";

describe("poker game test", () => {
  it("should be playing when all ready", () => {});
  it("should end normally", () => {
    const room = new Room();
  });
});
